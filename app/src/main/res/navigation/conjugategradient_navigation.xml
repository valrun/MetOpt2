<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/conjugategradient_navigation"
    app:startDestination="@id/conjugategradientFragment"
    tools:ignore="UnusedNavigation">
    <fragment
        android:id="@+id/conjugategradientFragment"
        android:name="com.example.metopt.fragments.ConjugateGradientFragment"
        android:label="GradientFragment"
        tools:layout="@layout/fragment_gradient">
        <action
            android:id="@+id/action_home_to_new"
            app:destination="@id/conjugategradientFragment" />
        <argument
            android:name="level"
            android:defaultValue="true"
            app:argType="boolean" />
        <argument
            android:name="coordinateLine"
            android:defaultValue="true"
            app:argType="boolean" />
        <argument
            android:name="axis"
            android:defaultValue="true"
            app:argType="boolean" />
    </fragment>
</navigation>
